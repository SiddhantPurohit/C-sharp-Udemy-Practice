@model IEnumerable<HospitalSimulation.Appoinment>

@{
    ViewBag.Title = "getAppoinments";
}

<h2>getAppoinments</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PatientName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.RepotedIssue)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AppoinmetDate)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PatientName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.RepotedIssue)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AppoinmetDate)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID })
        </td>
    </tr>
}

</table>
